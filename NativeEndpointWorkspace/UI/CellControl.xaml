<UserControl x:Class="NativeEndpointWorkspace.UI.CellControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             MinWidth="160" MinHeight="110">
    <Border BorderBrush="#5B677A" BorderThickness="1" Background="#20252D" CornerRadius="4">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="34"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid x:Name="Header" Grid.Row="0" Background="#2D3440">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Margin="5,4" Padding="7,2" Background="#445064" CornerRadius="3">
                    <TextBlock x:Name="CellNumberText" Foreground="White" FontWeight="Bold" VerticalAlignment="Center"/>
                </Border>
                <TextBlock x:Name="EndpointText" Grid.Column="1" Margin="6,0" Foreground="#E7EBF2" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                <Button x:Name="DetachButton" Grid.Column="2" Margin="2,4,5,4" Padding="8,0" Content="Detach" Click="DetachButton_Click"
                        ToolTip="Detach this endpoint from the Cell and leave the application open"/>
            </Grid>

            <Border x:Name="EndpointHost" Grid.Row="1" Margin="4" BorderBrush="#3D4655" BorderThickness="1" Background="#15191F" ClipToBounds="True">
                <TextBlock x:Name="EmptyText" Foreground="#8C98A8" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"
                           Text="No endpoint bound"/>
            </Border>

        </Grid>
    </Border>
</UserControl>
